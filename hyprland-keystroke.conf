# Keystroke launcher — Hyprland integration
# Source this from your hyprland.conf:
#   source = ~/.config/hypr/keystroke.conf

# Trigger binding
bindd = ALT, SPACE, Keystroke launcher, exec, keystroke

# Layer rules for blur effect
layerrule = blur, keystroke
layerrule = ignorealpha 0.3, keystroke
